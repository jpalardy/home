{
  "author": "Jonathan Palardy",
  "name": "home",
  "description": "The web pages at home.jpalardy.com",
  "version": "0.3.0",
  "scripts": {
    "build": "parcel build src/html/index.html -d public --public-url ./ --no-minify --no-cache",
    "watch": "parcel serve src/html/index.html -d public --no-cache",
    "coverage": "nyc --reporter=html --reporter text npm test",
    "test": "mocha --reporter dot"
  },
  "devDependencies": {
    "babel-core": "6.26.3",
    "babel-preset-env": "1.7.0",
    "less": "3.12.2",
    "mocha": "8.0.1",
    "nyc": "15.1.0",
    "parcel-bundler": "1.12.4"
  },
  "private": true
}
