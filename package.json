{
  "author": "Jonathan Palardy",
  "name": "home",
  "description": "The web pages at home.jpalardy.com",
  "version": "0.3.0",
  "scripts": {
    "build": "parcel build src/html/index.html -d public --public-url ./ --no-source-maps --no-cache",
    "watch": "parcel serve src/html/index.html -d public --no-source-maps --no-cache",
    "coverage": "nyc --reporter=html --reporter text npm test",
    "test": "mocha --reporter dot"
  },
  "devDependencies": {
    "babel-core": "6.26.3",
    "babel-preset-env": "1.7.0",
    "eslint": "7.19.0",
    "eslint-config-airbnb-base": "14.2.1",
    "eslint-plugin-import": "2.22.1",
    "mocha": "8.2.1",
    "nyc": "15.1.0",
    "parcel-bundler": "1.12.4"
  },
  "private": true
}
